#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Get repository root
REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Run TypeScript strict check
cd "$REPO_ROOT/frontend" && npm run typecheck:strict

# Run linter
cd "$REPO_ROOT/frontend" && npm run lint
